$(function() {
  $('.chart.yes').easyPieChart({
    barColor: 'green',
    lineWidth: 5,
    scaleLength: 30
  });
  $('.chart.no').easyPieChart({
    barColor: 'red',
    lineWidth: 5,
    scaleLength: 30
  });
});
