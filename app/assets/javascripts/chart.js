$(function() {
  $('.chart.yes').easyPieChart({
    barColor: 'green',
    lineWidth: 5,
    scaleLength: 10
  });
  $('.chart.no').easyPieChart({
    barColor: 'red',
    lineWidth: 5,
    scaleLength: 10
  });
});
